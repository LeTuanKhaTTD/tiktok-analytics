# Streamlit deployment dependencies (runtime only)

# Dashboard
streamlit>=1.28.0
plotly==5.18.0

# Data
pandas==2.1.4
numpy==1.26.2
openpyxl==3.1.2
xlsxwriter==3.1.9
reportlab==4.2.5

# AI/NLP used by dashboard
google-genai>=1.0.0

# Optional ML stack (PhoBERT local inference/training) is intentionally
# excluded from Streamlit Cloud runtime requirements because these packages
# often fail or timeout during cloud builds. The dashboard degrades gracefully
# when PhoBERT is unavailable.

# API clients used in project modules
google-api-python-client==2.110.0
google-auth==2.25.2
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
requests==2.31.0

# Optional utility libs imported by some modules
langdetect==1.0.9
vaderSentiment==3.3.2

# Notes:
# - Removed dev/test/lint packages from runtime requirements.
# - Removed older Vietnamese NLP packages (underthesea/vncorenlp/pyvi)
#   which commonly fail metadata build on cloud Linux/Python combinations.
